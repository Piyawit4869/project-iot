import { CreateProducts } from "@/components/features/products";
import React from "react";

const CreateContainer = () => {
  return (
    <div className="flex flex-col gap-4">
      <CreateProducts />
    </div>
  );
};

export default CreateContainer;
