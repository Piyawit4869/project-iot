export enum SidePanel {
  LEFT = "left",
  RIGHT = "right",
}

export enum RoomUserType {
  CUSTOMER_USER_DM = "customer_user_dm",
  GROUP = "group",
}

export enum ParticipantType {
  CUSTOMER = "customer",
}
